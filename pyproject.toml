[project]
name = "diet"
version = "0.1.0"
description = "The Bot app"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "psycopg2-binary>=2.9.10",
    "python-dotenv==1.0.1",
    "python-telegram-bot[job-queue,webhooks]==21.10",
]

[tool.uv]
dev-dependencies = [
    "ruff==0.7.0",
]